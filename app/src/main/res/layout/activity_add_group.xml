<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    tools:context=".AddGroupActivity">

    <!-- Top Bar -->
    <TextView
        android:id="@+id/topBarTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SplitSmart"
        android:textSize="24sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:background="#800020"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        />

    <!-- Back Button (top right corner) -->

    <!-- Main Content -->
    <ScrollView
        android:id="@+id/scrollContent"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@+id/topBarTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Add Group"
                android:textSize="24sp"
                android:textColor="#800020"
                android:textStyle="bold"/>

            <EditText
                android:id="@+id/groupNameInput"
                android:layout_width="360dp"
                android:layout_height="35dp"
                android:layout_marginTop="8dp"
                android:hint="Group Name"
                android:background="@drawable/input_bg"
                android:textColor="@color/text_primary"
                android:textColorHint="@color/text_secondary"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Select Members"
                android:textSize="16sp"
                android:textColor="#333333"
                android:layout_marginTop="16dp" />


            <CheckBox android:id="@+id/checkboxMeena" android:text="Meena" android:layout_width="91dp" android:layout_height="30dp" android:textColor="#333333" />
            <CheckBox android:id="@+id/checkboxNazneen" android:text="Nazneen" android:layout_width="91dp" android:layout_height="30dp" android:textColor="#333333" />
            <CheckBox android:id="@+id/checkboxJudith" android:text="Judith" android:layout_width="91dp" android:layout_height="30dp" android:textColor="#333333"/>
            <CheckBox android:id="@+id/checkboxJulie" android:text="Julie" android:layout_width="91dp" android:layout_height="30dp" android:textColor="#333333"/>

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/addGroupButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Add"
                android:backgroundTint="#800020"
                android:textColor="#FFFFFF"
                android:layout_marginTop="24dp"/>


            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/groupRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="117dp"
                android:background="@color/white"
                android:scrollbars="horizontal"
                tools:listitem="@layout/item_group" />


        </LinearLayout>
    </ScrollView>

    <Button
        android:id="@+id/homeBtn"
        android:layout_width="119dp"
        android:layout_height="36dp"
        android:layout_marginTop="660dp"
        android:layout_marginEnd="136dp"
        android:backgroundTint="#FFFFFF"
        android:text="Home"
        android:textColor="#800020"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/expenseBtn"
        android:layout_width="128dp"
        android:layout_height="34dp"
        android:layout_marginTop="660dp"
        android:layout_marginEnd="268dp"
        android:backgroundTint="#FFFFFF"
        android:text="Add expense"
        android:textColor="#800020"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/settingsBtn"
        android:layout_width="114dp"
        android:layout_height="36dp"
        android:layout_marginTop="660dp"
        android:layout_marginEnd="12dp"
        android:backgroundTint="#FFFFFF"
        android:text="settings"
        android:textColor="#800020"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>