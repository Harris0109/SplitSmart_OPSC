<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    tools:context=".SettingsActivity">

    <!-- Top Bar -->
    <TextView
        android:id="@+id/topBarTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SplitSmart"
        android:textSize="24sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:background="#800020"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Settings Title -->
    <TextView
        android:id="@+id/settings_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Settings"
        android:textStyle="bold"
        android:textColor="#800020"
        android:textSize="25sp"
        android:layout_marginStart="24dp"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/topBarTitle"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- ====== MAIN CONTENT ====== -->
    <ScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/settings_title"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <!-- ==== MULTI-LANGUAGE SECTION ==== -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/multi_language_label"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Multi-language"
                    android:textStyle="bold"
                    android:textColor="#000000"
                    android:textSize="18sp" />

                <androidx.appcompat.widget.SwitchCompat
                    android:id="@+id/multi_language_toggle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:trackTint="#800020"
                    android:thumbTint="#800020"
                    android:checked="true" />
            </LinearLayout>

            <TextView
                android:id="@+id/multi_language_description"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="Set to your language"
                android:textColor="#555555"
                android:textSize="13sp" />

            <Spinner
                android:id="@+id/language_spinner"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:popupBackground="#FFFFFF"
                android:entries="@array/languages_array"
                android:backgroundTint="@android:color/darker_gray" />

            <!-- ==== NOTIFICATIONS SECTION ==== -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginTop="20dp">

                <TextView
                    android:id="@+id/notifications_label"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Notifications"
                    android:textStyle="bold"
                    android:textColor="#000000"
                    android:textSize="18sp" />

                <androidx.appcompat.widget.SwitchCompat
                    android:id="@+id/notifications_toggle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:trackTint="#E8C0C0"
                    android:thumbTint="#800020"
                    android:checked="true" />
            </LinearLayout>

            <TextView
                android:id="@+id/notifications_description"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="Receive alerts and updates"
                android:textColor="#555555"
                android:textSize="13sp" />

            <!-- ==== OFFLINE SYNC SECTION ==== -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginTop="20dp">

                <TextView
                    android:id="@+id/offline_sync_label"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Offline Sync"
                    android:textStyle="bold"
                    android:textColor="#000000"
                    android:textSize="18sp" />

                <androidx.appcompat.widget.SwitchCompat
                    android:id="@+id/offline_sync_toggle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:trackTint="#CCCCCC"
                    android:thumbTint="#888888"
                    android:checked="false" />
            </LinearLayout>

            <TextView
                android:id="@+id/offline_sync_description"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="Enable data synchronisation"
                android:textColor="#555555"
                android:textSize="13sp" />

        </LinearLayout>
    </ScrollView>

    <Button
        android:id="@+id/homeBtn"
        android:layout_width="119dp"
        android:layout_height="36dp"
        android:layout_marginTop="660dp"
        android:layout_marginEnd="136dp"
        android:backgroundTint="#FFFFFF"
        android:text="Home"
        android:textColor="#800020"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/expenseBtn"
        android:layout_width="128dp"
        android:layout_height="34dp"
        android:layout_marginTop="660dp"
        android:layout_marginEnd="268dp"
        android:backgroundTint="#FFFFFF"
        android:text="Add expense"
        android:textColor="#800020"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/groupBtn"
        android:layout_width="114dp"
        android:layout_height="36dp"
        android:layout_marginTop="660dp"
        android:layout_marginEnd="12dp"
        android:backgroundTint="#FFFFFF"
        android:text="Add group"
        android:textColor="#800020"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>

